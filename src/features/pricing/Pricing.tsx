import { Box, Typography } from '@mui/material';

export default function Pricing() {
  return (
    <Box>
      <Typography variant="h5" gutterBottom>
        Preisinformation
      </Typography>
      <Typography>
        Preisübersicht und Details werden hier angezeigt.
      </Typography>
    </Box>
  );
}
